# SparkPos Local Development Configuration (Codespace)
# Variables like ${SUPABASE_ANON_KEY} are substituted by setup.sh

# Supabase Local Instance
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
SUPABASE_SYNC_URL=http://127.0.0.1:54321
SUPABASE_CATALOG_URL=http://127.0.0.1:54321

# Database (for migrations)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# PowerSync - local self-hosted instance
POWERSYNC_URL=http://localhost:8080
POWERSYNC_PRIVATE_KEY=

# App Environment
APP_ENV=development

# Finix Payment (optional - not needed for local dev)
FINIX_DEFAULT_DEVICE_ID=
FINIX_DEFAULT_MERCHANT_ID=
FINIX_DEFAULT_DEVICE_NAME=
FINIX_DEFAULT_SERIAL_NUMBER=
