# RequestManager Local Development Configuration (Codespace)
# Variables like ${SUPABASE_ANON_KEY} are substituted by setup.sh

# Supabase connection (local)
SPARK_POS_SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

# API key for SparkPos edge functions to call RequestManager
REQUEST_MANAGER_API_KEY=dev-request-manager-key-123
